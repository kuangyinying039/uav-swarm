# 距离捕获奖励（当前版本）

配置：`quadrotor_pursuit_env.py` 的 QuadrotorPursuitConfig；公式实现：`pursuit_rewards.py`。
捕获仍为任一有效无人机与目标的三维中心距离 ≤1 m。没有自动课程。

所有分项最终相加为共享团队奖励，各机的接近进展另输出为诊断，不是独立个体 PPO 奖励。

令 d_i 为真实三维距离，r 为捕获距离，L_i=log(max(d_i,r)/r)。

| 项 | 默认公式/权重 |
|---|---|
| 每架无人机接近 | 2 × mean(clip(L_i(t-1)-L_i(t),-1,1))；失效机贡献置零，分母保持总机数 |
| 靠近目标 | 0.02 × mean(1/(1+L_i))；失效机为零 |
| 围捕进展 | 2 × (S(t)-S(t-1)) |
| 障碍接近 | -0.2 × 各机最近建筑风险的平均值 |
| 捕获/超时/时间 | +100 / -50 / 每步 -0.05 |
| 碰撞/安全干预/控制拒绝 | 原有 -10、-0.1、-0.2 系数保留 |
| 动作平滑 | -0.01 × 平均动作变化平方和 |
| 信息增益/估计不确定性/通信 | 默认权重置零，诊断保留 |

围捕分数 S∈[0,1]：先按目标周围水平角排序，最大角空隙为 G；角度分数为 clip((2π-G)/(2π(1-1/n)),0,1)。乘以 exp(-平均捕获距离差/6 m) 和 exp(-平均高度差/2 m)。至少需要三架有效机且水平方位可定义，否则为零。它是包夹几何代理，不保证封死所有逃逸路线，也不要求三机同时命中。

建筑风险：计算无人机中心到三维建筑长方体的距离，减去机体安全半径 0.45 m，截到非负为 c。风险为 log(1+max(1.5-c,0)/0.1)/log(1+1.5/0.1)。远离安全带为零，贴近建筑上限为 1；从足够高的建筑上空经过不会被当成穿墙。无建筑时此项为零。连续控制安全检查仍处理碰撞，不依赖奖励代替安全检查。

设计限制：接近和围捕项允许负奖励，避免只奖励接近、不惩罚退后的往复套利。围捕不发持续静态奖励。靠近项上限 0.02 小于时间代价 0.05，静止不捕获不能靠此持续获得正收益。这些差分不是严格含折扣势函数，不声明最优策略不变，也不能保证不存在其他奖励利用方式。

保留可选成功示范行为克隆与 DAgger 纠偏，统称示范辅助 MAPPO。预训练仅更新策略，不预训练价值网络。用户可后续决定采用示范或课程；此轮不自动运行任何一种训练。新奖励实验请使用新输出目录；旧环境配置中的 capture_approach_progress_weight 已移除，不应混用旧检查点。
