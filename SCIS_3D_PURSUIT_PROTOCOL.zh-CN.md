> 历史协议：本文记录旧 NMPC/FOV 实验，数值不代表当前实现。当前围捕已统一为速度＋偏航角速率飞控与 MID-360；运行和评估请使用 [当前围捕指南](PURSUIT_DEMO_GUIDE.zh-CN.md)，旧动力学基准需要在新环境下重新评估。

# SCIS 三维 3-v-1 追逃实验统一口径

本文件是论文训练、消融和对比实验的唯一正式围捕口径。项目不再提供二维围捕训练或评估入口；三维环境复用的平面几何与跟踪代码只是内部公共实现。

## 1. 正式任务

搜索模块发现目标并交接一个带噪初始航迹后，三架四旋翼在目标周围以三维近似等边三角形初始化，各机到目标的三维距离为 4--6 m。初始化搜索保证至少一架无人机对目标满足距离、水平/垂直 FOV 和建筑 LOS，代表刚刚完成直接观测。随后三架无人机在三维城市建筑棱柱环境中追捕一个逃逸目标。默认逃逸方是具有全局态势信息的规则型 occlusion-seeking 目标：它读取三架追捕机的真实三维位置和全部建筑几何，在候选侧面、背面及屋顶航点中选择兼顾遮挡、距离和机动代价的方向。

捕获采用“多机协同追捕、任意单机完成最终网捕”。每架追捕机机体下方安装一个随姿态旋转的刚性圆形网盘；只要逃逸目标中心进入任意一张网盘即终止。参考论文仅以符号 `R` 表示网半径而未报告数值，本项目暂设半径 1.0 m、网心沿机体负 z 轴偏置 0.45 m、法向容差 0.30 m，命中一次即可捕获。建筑同时影响视线、目标逃逸航点和飞行安全。

当前论文阶段不设置动态空中障碍物（`n_obstacles=0` 且 `dynamic_obstacles_enabled=False`）；环境中只保留静态建筑棱柱。动态障碍代码仅作为后续扩展，不进入训练、消融或对比实验。

## 2. 所提方法

围捕从搜索模块交接的带噪目标航迹开始，目标航迹对所有有效追捕机持续可用，但策略不读取目标真值。每架无人机维护六维常速度 Kalman 滤波状态 `[x,y,z,vx,vy,vz]`；通信链路可传输并融合各机的带噪航迹。HGAT 编码异构局部实体和通信图，MAPPO 输出四维归一化参考 `[vx_ref,vy_ref,vz_ref,yaw_rate_ref]`。非线性 MPC 在完整四旋翼状态 `[p3,v3,q4,omega3]` 上求解总推力和三轴力矩 `[T,tau_x,tau_y,tau_z]`。

正式训练必须从随机初始化开始：三维任务的单机观测为 53 维、连续动作为 4 维，二维检查点不允许迁移或恢复。默认网络仍采用 128 维隐藏层、2 层 4 头 HGAT；这些是三维网络的新参数，而不是二维参数重用。

## 3. 三维对比方法

- `APF-3D`：目标吸引势、追捕机间排斥势、建筑棱柱排斥势和边界排斥势，并设置错开的接近方位以促进包围。
- `FRPN-3D`：根据三维相对位置、相对速度、视线角速度和闭合速度生成比例导引加速度，并叠加快速直接响应项。

两种方法不得读取目标真值。每架追捕机只能使用与所提方法相同的本地六维 KF 航迹及 freshness-aware CI 融合结果，并经历相同的局部 FOV、建筑遮挡、通信拓扑和链路中断。它们只替换 HGAT-MAPPO 决策层，均通过与所提方法完全相同的 NMPC、四旋翼动力学、推力/力矩约束和安全约束执行。

## 4. 消融与指标

所有消融都在相同三维场景和种子上完成：去 HGAT、去 freshness-aware CI、去通信/设置通信中断、去 NMPC 安全约束，以及规则权重敏感性。核心指标为捕获率、成功样本捕获时间、删失时间、可见率、目标丢失/重捕获次数、碰撞数、安全干预数、NMPC 可行率、航程和累计回报。

场景难度先由 APF-3D 与 FRPN-3D 的多种子捕获率和可见率判断；若二者均接近 0%，优先减小建筑密度、目标速度或遮挡收益；若均接近 100%，则反向调整。正式结论不得使用快速冒烟配置。

首轮正式 NMPC 筛查采用遮挡收益权重 1.8，APF/FRPN 在 5 个种子中均为零捕获，而 NMPC 可行率已达到 95.6%/99.7%。因此第二轮仅将遮挡收益权重降至 1.2；目标速度、建筑数量、建筑尺度及其他逃逸权重保持不变。

由于权重 1.2 未改变这些种子中的离散候选排序，进一步加入遮挡航点滞后：候选航点至少保持 10 个决策步，只有到达当前航点，或驻留期结束且新候选得分至少高 0.35 时才切换。这限制了规则目标逐步瞬时重规划的非物理优势。

## 5. 运行入口

正式训练默认入口：

```powershell
python .\repro\train_multi_seed_cooperative_marl.py --algo mappo --episodes 2000
```

对比方法与场景难度评估：

```powershell
python .\repro\evaluate_3d_baselines.py --steps 300 --seeds 23 37 51 71 89
```

输出 JSON 保存汇总和逐回合记录，同名 CSV 用于 MATLAB 或 Python 绘制论文图。MATLAB 不是必须工具。

## 6. HGAT-MAPPO 三维训练环境

不使用课程学习。训练从第一回合起就采用与 APF-3D、FRPN-3D 对比实验相同的三维场景配置：完整建筑、occlusion-seeking 逃逸目标、相同通信配置、完整四旋翼动力学和 NMPC。训练与对比方法只替换决策器，环境难度和约束保持一致；显式命令行环境参数仍优先于默认值。

奖励以终局网捕为主，并增加最近网盘对目标的势函数差分奖励。成功网捕奖励 100，达到时限仍未捕获惩罚 50，每步时间代价为 0.05。由于围捕从带噪航迹交接开始，检测、可见性、丢失和重捕获仅作为诊断指标，不进入奖励；逐步围堵和固定有效航迹奖励也被移除，避免策略通过拖延终止持续刷取正回报。航迹信息增益只保留 0.10 的辅助权重，原始增益先截断到 1.0，因此每步最多贡献 0.10；同时保留通信、碰撞、NMPC 不可行和动作平滑代价。当前对比协议不包含随机永久失效，相关概率固定为零。
